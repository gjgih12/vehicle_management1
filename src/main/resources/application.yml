
# Tomcat配置
server:
  tomcat:
    uri-encoding: UTF-8           # tomcat的URI编码
    max-threads: 1024             # tomcat最大线程数，默认为200
    min-spare-threads: 30         #:最小备用线程数
    accept-count: 5000            #当tomcat起动的线程数达到最大时，接受排队的请求个数，默认值为100
  port: 9001
  servlet:
    context-path: /vehicle_management

mybatis:
  mapper-locations: mapper/*.xml

spring:
  profiles:
    active: dev
  jackson:
    time-zone: GMT+8      #时差问题

logging:
#  config:
#    classpath: log/logback-spring.xml
  file: D:/nmyslog/nmys/log_info.log
#    path:
#  level:
#    com.gj: debug      #默认是info

jasypt:
  encryptor:
    password: Uy&75loRd%mDssx  #你的秘钥

config:
  gexin:
    host: http://sdk.open.api.igexin.com/apiex.htm
    appId: NEW6Xa9TuC7lTUoepMy45A
    appKey: KlyrHUVdl16IGeCdS7C3r2
    masterSecret: h9bqx87bIx7a70mf6HP4g6